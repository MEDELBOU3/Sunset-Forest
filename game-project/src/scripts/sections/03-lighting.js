export default "        // =========================================================\n        // LIGHTING\n        // =========================================================\n        scene.add(new THREE.HemisphereLight(0xf7cfe0, 0x1f2e1a, 0.5));\n        const sunLight = new THREE.DirectionalLight(0xffd6a5, 2.2);\n        sunLight.position.set(80, 25, 100);\n        sunLight.castShadow = true;\n        const d = 120;\n        sunLight.shadow.camera.left = -d; sunLight.shadow.camera.right = d;\n        sunLight.shadow.camera.top = d; sunLight.shadow.camera.bottom = -d;\n        sunLight.shadow.mapSize.set(2048, 2048);\n        sunLight.shadow.bias = -0.0004; sunLight.shadow.radius = 2;\n        scene.add(sunLight);\n        const rimLight = new THREE.DirectionalLight(0xc8a8d4, 0.5);\n        rimLight.position.set(-80, 20, -60);\n        scene.add(rimLight);\n        scene.add(new THREE.AmbientLight(0x1a1828, 0.35));\n        const GROUND_Y = -5;";

