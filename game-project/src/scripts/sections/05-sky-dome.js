export default "        // =========================================================\n        // SKY DOME\n        // =========================================================\n        const skyGeo = new THREE.SphereGeometry(400, 32, 16);\n        skyGeo.scale(-1, 1, -1);\n        scene.add(new THREE.Mesh(skyGeo, new THREE.ShaderMaterial({\n            uniforms: {\n                topColor: { value: new THREE.Color(0x7b8fc7) },\n                bottomColor: { value: new THREE.Color(0xf9c8dc) },\n                offset: { value: 18 }, exponent: { value: 0.6 }\n            },\n            vertexShader: `varying vec3 vWorldPos; void main() { vec4 wp = modelMatrix * vec4(position, 1.0); vWorldPos = wp.xyz; gl_Position = projectionMatrix * viewMatrix * wp; }`,\n            fragmentShader: `uniform vec3 topColor, bottomColor; uniform float offset, exponent; varying vec3 vWorldPos; void main() { float h = normalize(vWorldPos + vec3(0, offset, 0)).y; gl_FragColor = vec4(mix(bottomColor, topColor, max(pow(max(h, 0.0), exponent), 0.0)), 1.0); }`,\n            side: THREE.BackSide\n        })));\n\n        const thermalRenderer = new THREE.WebGLRenderTarget(\n            window.innerWidth, window.innerHeight,\n            { minFilter: THREE.LinearFilter, magFilter: THREE.LinearFilter }\n        );\n        const thermalCanvas = document.getElementById(\u0027thermal-canvas\u0027);\n        const thermalCtx = thermalCanvas.getContext(\u00272d\u0027);\n        const THERMAL_LAYER = 1;\n\n        // Material that renders zombies solid red â€” used only during thermal pass\n        const thermalMat = new THREE.MeshBasicMaterial({ color: 0xff0000 });\n\n        // =============================================================\n        //  COLLISION SYSTEM â€” Drop this block right after your\n        //  \"GROUND\" section, before PARTICLES.\n        //  Uses spatial-hash broad-phase + AABB narrow-phase.\n        //  Zero external dependencies. Zero physics engine.\n        // =============================================================";

