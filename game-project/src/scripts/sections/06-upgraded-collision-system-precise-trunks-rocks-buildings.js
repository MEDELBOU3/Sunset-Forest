export default "        // =============================================================\n        //  UPGRADED COLLISION SYSTEM (Precise Trunks, Rocks, Buildings)\n        // =============================================================\n        const CollisionSystem = (() =\u003e {\n            const PLAYER_R = 0.55;\n            const ZOMBIE_R = 0.45;\n            const VEHICLE_HW = 2.4;  // Slightly thinner vehicle collision to prevent getting stuck\n            const VEHICLE_HL = 4.6;\n            const CELL = 10;\n\n            const _grid = new Map();\n            const _statics = [];\n            const _tmpV3 = new THREE.Vector3();\n            const _tmpBox = new THREE.Box3();\n            const _tmpSize = new THREE.Vector3();\n\n            function _key(ix, iz) { return ((ix \u0026 0x7FFF) | ((iz \u0026 0x7FFF) \u003c\u003c 15)); }\n\n            function _insert(entry) {\n                const x0 = Math.floor((entry.minX - 1) / CELL);\n                const x1 = Math.floor((entry.maxX + 1) / CELL);\n                const z0 = Math.floor((entry.minZ - 1) / CELL);\n                const z1 = Math.floor((entry.maxZ + 1) / CELL);\n                for (let ix = x0; ix \u003c= x1; ix++) {\n                    for (let iz = z0; iz \u003c= z1; iz++) {\n                        const k = _key(ix, iz);\n                        let cell = _grid.get(k);\n                        if (!cell) { cell = []; _grid.set(k, cell); }\n                        cell.push(entry);\n                    }\n                }\n            }\n\n            function _nearby(x, z, r, out) {\n                const x0 = Math.floor((x - r) / CELL);\n                const x1 = Math.floor((x + r) / CELL);\n                const z0 = Math.floor((z - r) / CELL);\n                const z1 = Math.floor((z + r) / CELL);\n                for (let ix = x0; ix \u003c= x1; ix++) {\n                    for (let iz = z0; iz \u003c= z1; iz++) {\n                        const cell = _grid.get(_key(ix, iz));\n                        if (cell) for (const e of cell) out.add(e);\n                    }\n                }\n            }\n\n            // --- SMART REGISTRATION ---\n            // Applies different collision shapes based on the object category\n            function registerMesh(mesh, category = \u0027Other\u0027) {\n                // Completely ignore soft foliage\n                if (category === \u0027Grass\u0027 || category === \u0027Bushes\u0027 || category === \u0027Extras\u0027) return;\n\n                _tmpBox.setFromObject(mesh);\n                const cx = (_tmpBox.min.x + _tmpBox.max.x) * 0.5;\n                const cz = (_tmpBox.min.z + _tmpBox.max.z) * 0.5;\n                _tmpBox.getSize(_tmpSize);\n\n                let entry;\n\n                if (category === \u0027Trees\u0027) {\n                    // Trees get a precise cylinder collision (just the trunk)\n                    const trunkRadius = 0.65 * mesh.scale.x;\n                    entry = {\n                        type: \u0027circle\u0027,\n                        cx: mesh.position.x, cz: mesh.position.z, // Trunk is exactly at pivot point\n                        r: trunkRadius, bsr: trunkRadius,\n                        minX: mesh.position.x - trunkRadius, maxX: mesh.position.x + trunkRadius,\n                        minZ: mesh.position.z - trunkRadius, maxZ: mesh.position.z + trunkRadius\n                    };\n                }\n                else if (category === \u0027Rocks\u0027) {\n                    // Rocks get rounded circular collision (prevents getting snagged on corners)\n                    const r = Math.max(_tmpSize.x, _tmpSize.z) * 0.35;\n                    entry = {\n                        type: \u0027circle\u0027,\n                        cx: cx, cz: cz, r: r, bsr: r,\n                        minX: cx - r, maxX: cx + r,\n                        minZ: cz - r, maxZ: cz + r\n                    };\n                }\n                else {\n                    // Buildings and Cars get standard Box collision (shrunk slightly to ignore roofs)\n                    const shrinkX = category === \u0027buildings\u0027 ? _tmpSize.x * 0.15 : 0;\n                    const shrinkZ = category === \u0027buildings\u0027 ? _tmpSize.z * 0.15 : 0;\n                    const bsr = Math.sqrt((_tmpSize.x / 2) ** 2 + (_tmpSize.z / 2) ** 2);\n\n                    entry = {\n                        type: \u0027box\u0027,\n                        cx: cx, cz: cz, bsr: bsr,\n                        minX: _tmpBox.min.x + shrinkX, maxX: _tmpBox.max.x - shrinkX,\n                        minZ: _tmpBox.min.z + shrinkZ, maxZ: _tmpBox.max.z - shrinkZ\n                    };\n                }\n\n                _statics.push(entry);\n                _insert(entry);\n            }\n\n            const _checked = new Set();\n            function resolvePoint(pos, r) {\n                _checked.clear();\n                _nearby(pos.x, pos.z, r + 1.0, _checked);\n\n                for (const e of _checked) {\n                    const dx = pos.x - e.cx;\n                    const dz = pos.z - e.cz;\n                    const distSq = dx * dx + dz * dz;\n\n                    if (distSq \u003e (e.bsr + r) * (e.bsr + r)) continue;\n\n                    if (e.type === \u0027circle\u0027) {\n                        // Smooth sliding against round objects (Trees/Rocks)\n                        const dist = Math.sqrt(distSq);\n                        const overlap = (e.r + r) - dist;\n                        if (overlap \u003e 0 \u0026\u0026 dist \u003e 0) {\n                            pos.x += (dx / dist) * overlap;\n                            pos.z += (dz / dist) * overlap;\n                        }\n                    } else if (e.type === \u0027box\u0027) {\n                        // Sliding against flat walls (Buildings)\n                        const margin = r;\n                        const eMinX = e.minX - margin, eMaxX = e.maxX + margin;\n                        const eMinZ = e.minZ - margin, eMaxZ = e.maxZ + margin;\n\n                        if (pos.x \u003c= eMinX || pos.x \u003e= eMaxX) continue;\n                        if (pos.z \u003c= eMinZ || pos.z \u003e= eMaxZ) continue;\n\n                        const oPX = eMaxX - pos.x, oNX = pos.x - eMinX;\n                        const oPZ = eMaxZ - pos.z, oNZ = pos.z - eMinZ;\n                        const minOX = Math.min(oPX, oNX), minOZ = Math.min(oPZ, oNZ);\n\n                        if (minOX \u003c minOZ) pos.x += (oPX \u003c oNX) ? oPX : -oNX;\n                        else pos.z += (oPZ \u003c oNZ) ? oPZ : -oNZ;\n                    }\n                }\n            }\n\n            // ... (keep separateZombies exactly as it was) ...\n            const SEP_DIST_SQ = (ZOMBIE_R * 2) ** 2;\n            function separateZombies(zombieList, playerPos) {\n                const CHECK_R_SQ = 35 * 35;\n                for (let i = 0; i \u003c zombieList.length; i++) {\n                    const a = zombieList[i];\n                    if (a.isDead || !a.isAlerted) continue;\n                    const apx = a.mesh.position.x, apz = a.mesh.position.z;\n                    const tpx = apx - playerPos.x, tpz = apz - playerPos.z;\n                    if (tpx * tpx + tpz * tpz \u003e CHECK_R_SQ) continue;\n\n                    for (let j = i + 1; j \u003c zombieList.length; j++) {\n                        const b = zombieList[j];\n                        if (b.isDead) continue;\n                        const ddx = apx - b.mesh.position.x, ddz = apz - b.mesh.position.z;\n                        const distSq = ddx * ddx + ddz * ddz;\n                        if (distSq \u003e= SEP_DIST_SQ || distSq \u003c 0.0001) continue;\n                        const dist = Math.sqrt(distSq);\n                        const push = (ZOMBIE_R * 2 - dist) / dist * 0.5;\n                        a.mesh.position.x += ddx * push; a.mesh.position.z += ddz * push;\n                        b.mesh.position.x -= ddx * push; b.mesh.position.z -= ddz * push;\n                    }\n                }\n            }\n\n            const _corners = [new THREE.Vector3(), new THREE.Vector3(), new THREE.Vector3(), new THREE.Vector3()];\n            function resolveVehicle(car) {\n                if (!car.root || !car.loaded) return;\n                const root = car.root;\n                root.updateMatrixWorld(false);\n                const hw = VEHICLE_HW, hl = VEHICLE_HL;\n                const offsets = [[hw, hl], [-hw, hl], [-hw, -hl], [hw, -hl]];\n                let maxPushX = 0, maxPushZ = 0;\n\n                for (let ci = 0; ci \u003c 4; ci++) {\n                    _tmpV3.set(offsets[ci][0], 0, offsets[ci][1]);\n                    _tmpV3.applyQuaternion(root.quaternion);\n                    _tmpV3.add(root.position);\n                    const cp = { x: _tmpV3.x, z: _tmpV3.z };\n                    const beforeX = cp.x, beforeZ = cp.z;\n\n                    resolvePoint(cp, 0.4); // slightly thicker probe for cars\n\n                    maxPushX += (cp.x - beforeX) * 0.25;\n                    maxPushZ += (cp.z - beforeZ) * 0.25;\n                }\n\n                if (Math.abs(maxPushX) \u003e 0.001 || Math.abs(maxPushZ) \u003e 0.001) {\n                    root.position.x += maxPushX;\n                    root.position.z += maxPushZ;\n\n                    // Brutal speed bleed on collision (simulates hitting a tree)\n                    car.speed *= 0.1;\n                }\n            }\n\n            return { PLAYER_R, ZOMBIE_R, registerMesh, resolvePoint, separateZombies, resolveVehicle };\n        })();\n\n\n        // =============================================================\n        //  INTEGRATION GUIDE â€” What to modify in your existing code:\n        //\n        //  1. scatterObjects()  â€” register each placed instance\n        //  2. tileObjects()     â€” register each placed tile\n        //  3. loadArmoredCar()  â€” register the car body itself\n        //  4. animate()         â€” resolve player + zombie + vehicle\n        //  5. updateZombies()   â€” add zombie-world collision call\n        // =============================================================\n\n\n        // â”€â”€ 1. PATCH scatterObjects â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€\n        // Add ONE line at the end of the inner for-loop,\n        // right after  scene.add(instance) :\n        //\n        //     CollisionSystem.registerMesh(instance);\n        //\n        // Full replacement (drop-in):\n\n        /*\n        function scatterObjects(gltf, count, baseRadius, yOffset, isClustered, castShadows) {\n            let clusterCenters = [];\n            if (isClustered) {\n                const numClusters = Math.max(1, Math.floor(count / 5));\n                for (let i = 0; i \u003c numClusters; i++)\n                    clusterCenters.push({ x: (Math.random() - .5) * 160, z: (Math.random() - .5) * 160 });\n            }\n            for (let i = 0; i \u003c count; i++) {\n                const scale = 0.6 + Math.random() * 1.0;\n                const radius = baseRadius * scale;\n                let x, z, tooClose = true, attempts = 0;\n                while (tooClose \u0026\u0026 attempts \u003c 50) {\n                    if (isClustered \u0026\u0026 clusterCenters.length \u003e 0) {\n                        const center = clusterCenters[i % clusterCenters.length];\n                        const r = Math.random() * 8, theta = Math.random() * Math.PI * 2;\n                        x = center.x + r * Math.cos(theta); z = center.z + r * Math.sin(theta);\n                    } else { x = (Math.random() - .5) * 180; z = (Math.random() - .5) * 180; }\n                    tooClose = false;\n                    if (radius \u003e 0) for (const pos of placedPositions) {\n                        const dist = Math.hypot(pos.x - x, pos.z - z);\n                        if (pos.r \u003e 0 \u0026\u0026 dist \u003c pos.r + radius) { tooClose = true; break; }\n                    }\n                    attempts++;\n                }\n                if (attempts \u003e= 50) continue;\n                if (radius \u003e 0) placedPositions.push({ x, z, r: radius });\n\n                const instance = gltf.scene.clone();\n                instance.position.set(x, yOffset, z);\n                instance.rotation.y = Math.PI * 2 * Math.random();\n                if (isClustered) {\n                    instance.rotation.x = (Math.random() - .5) * .1;\n                    instance.rotation.z = (Math.random() - .5) * .1;\n                }\n                instance.scale.set(scale, scale, scale);\n                instance.matrixAutoUpdate = false;\n                instance.updateMatrix();\n                instance.traverse(node =\u003e {\n                    if (node.isMesh) {\n                        if (castShadows) node.castShadow = true;\n                        node.receiveShadow = true;\n                        if (node.material) node.material.side = THREE.DoubleSide;\n                    }\n                });\n                scene.add(instance);\n\n                // â–º REGISTER with collision system (skip tiny grass/flowers â€“ filtered inside)\n                CollisionSystem.registerMesh(instance);\n            }\n        }\n        */\n\n        // â”€â”€ 2. PATCH tileObjects â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€\n        // Ground tiles don\u0027t block movement, so we skip them.\n        // No change needed for tileObjects().\n\n\n        // â”€â”€ 3. PATCH loadArmoredCar â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€\n        // After  scene.add(root);  add:\n        //     CollisionSystem.registerMesh(root);\n        //\n        // (The armored car body becomes a static obstacle when not being driven.)\n\n\n        // â”€â”€ 4. PATCH animate() â€” player movement â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€\n        // Inside the  else { }  block that handles on-foot movement,\n        // AFTER  controls.moveRight / controls.moveForward,\n        // ADD these three lines:\n\n        /*\n            // â”€â”€ World Collision â€” player â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€\n            const _playerProbe = { x: camera.position.x, z: camera.position.z };\n            CollisionSystem.resolvePoint(_playerProbe, CollisionSystem.PLAYER_R);\n            camera.position.x = _playerProbe.x;\n            camera.position.z = _playerProbe.z;\n        */\n\n\n        // â”€â”€ 5. PATCH updateZombies() â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€\n        // Inside the per-zombie loop, AFTER the movement block\n        // (the  if (dist \u003e ZOMBIE_ATTACK_RANGE)  branch),\n        // ADD zombie-world collision:\n\n        /*\n            // â”€â”€ World Collision â€” zombie â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€\n            if (z.isAlerted) {\n                const zProbe = { x: z.mesh.position.x, z: z.mesh.position.z };\n                CollisionSystem.resolvePoint(zProbe, CollisionSystem.ZOMBIE_R);\n                z.mesh.position.x = zProbe.x;\n                z.mesh.position.z = zProbe.z;\n            }\n        */\n\n        // At the END of updateZombies(), ADD zombie-zombie separation:\n        /*\n            CollisionSystem.separateZombies(zombies, camera.position);\n        */\n\n\n        // â”€â”€ 6. PATCH updateArmoredCar() â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€\n        // At the END of updateArmoredCar(), AFTER position clamping, ADD:\n        /*\n            CollisionSystem.resolveVehicle(armoredCar);\n        */";

