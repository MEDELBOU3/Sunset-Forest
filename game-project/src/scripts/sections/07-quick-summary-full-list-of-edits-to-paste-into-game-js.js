export default "        // =============================================================\n        //  QUICK SUMMARY â€” full list of edits to paste into game.js\n        // =============================================================\n        /*\n        \n          â‘  After GROUND section â€” paste the full CollisionSystem block above.\n        \n          â‘¡ In scatterObjects() â€” replace function body with patched version above.\n        \n          â‘¢ In loadArmoredCar(), after scene.add(root):\n                CollisionSystem.registerMesh(root);\n        \n          â‘£ In animate(), inside the on-foot else block, after controls.moveForward:\n                const _playerProbe = { x: camera.position.x, z: camera.position.z };\n                CollisionSystem.resolvePoint(_playerProbe, CollisionSystem.PLAYER_R);\n                camera.position.x = _playerProbe.x;\n                camera.position.z = _playerProbe.z;\n        \n          â‘¤ In updateZombies(), inside the per-zombie loop after movement:\n                if (z.isAlerted) {\n                    const zp = { x: z.mesh.position.x, z: z.mesh.position.z };\n                    CollisionSystem.resolvePoint(zp, CollisionSystem.ZOMBIE_R);\n                    z.mesh.position.x = zp.x;\n                    z.mesh.position.z = zp.z;\n                }\n        \n          â‘¥ At the END of updateZombies(), before the closing brace:\n                CollisionSystem.separateZombies(zombies, camera.position);\n        \n          â‘¦ At the END of updateArmoredCar(), after clamping:\n                CollisionSystem.resolveVehicle(armoredCar);\n        \n        */";

