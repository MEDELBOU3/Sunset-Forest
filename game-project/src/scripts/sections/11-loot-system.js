export default "        // =========================================================\n        // LOOT SYSTEM\n        // =========================================================\n\n        // --- Model cache: each GLB is loaded only once ---\n        const lootModelCache = new Map(); // key â†’ THREE.Group (template)\n        preloadAllWeaponModels();\n        const lootModelLoadQueue = new Map(); // key â†’ array of callbacks waiting\n\n        function getCachedModel(key, path, onReady) {\n            if (lootModelCache.has(key)) {\n                onReady(lootModelCache.get(key).clone(true));\n                return;\n            }\n            if (lootModelLoadQueue.has(key)) {\n                lootModelLoadQueue.get(key).push(onReady);\n                return;\n            }\n            lootModelLoadQueue.set(key, [onReady]);\n            new GLTFLoader().load(path, (gltf) =\u003e {\n                lootModelCache.set(key, gltf.scene);\n                for (const cb of lootModelLoadQueue.get(key)) {\n                    cb(gltf.scene.clone(true));\n                }\n                lootModelLoadQueue.delete(key);\n            }, undefined, err =\u003e console.warn(`Loot model load failed: ${path}`, err));\n        }\n\n        let playerHasScope = false;\n        let thermalActive = false;\n        const thermalOriginalMaterials = new Map();\n\n        const thermalZombieMat = new THREE.MeshBasicMaterial({\n            color: 0xff3300,\n            depthTest: true,\n            depthWrite: true,\n            side: THREE.DoubleSide,\n            fog: false,\n        });\n        const SCOPE_LOOT_LIST = []; // tracks scope pickups on map\n        const _lootRayDirection = new THREE.Vector3();\n        const _lootToTarget = new THREE.Vector3();\n        const _lootTargetPos = new THREE.Vector3();\n\n        function playLootPickupSound() {\n            if (!lootPickupSound.buffer) return;\n            if (lootPickupSound.isPlaying) lootPickupSound.stop();\n            lootPickupSound.play();\n        }\n\n        function markZombieThermalLayer(root) {\n            root.layers.enable(THERMAL_LAYER);\n            root.traverse(node =\u003e {\n                node.layers.enable(THERMAL_LAYER);\n            });\n        }\n\n\n        // --- Active world loot list ---\n        // Each entry: { mesh, hitBox, type, data, bobPhase }\n        // type: \u0027weapon\u0027 | \u0027ammo\u0027\n        // data for weapon: { weaponKey, name }\n        // data for ammo:   { weaponKey, label, amount }\n        const worldLoot = [];\n        const lootHitboxes = [];\n\n        // --- Ammo type definitions ---\n        const ammoLootTypes = {\n            pistol: { label: \u00279mm Ammo\u0027, color: 0xffcc44, amount: 14, weaponKey: \u00271911\u0027, glowColor: 0xffaa00 },\n            shotgun: { label: \u0027Shotgun Shells\u0027, color: 0xff6622, amount: 5, weaponKey: \u0027shotgun\u0027, glowColor: 0xff4400 },\n            rifle: { label: \u00277.62mm Ammo\u0027, color: 0x44aaff, amount: 30, weaponKey: \u0027AK-47\u0027, glowColor: 0x0088ff },\n            smg: { label: \u00279mm SMG Ammo\u0027, color: 0x88ff44, amount: 25, weaponKey: \u0027mp5\u0027, glowColor: 0x44ff00 },\n        };\n\n        // --- Spawn a weapon loot item in the world ---\n        function spawnWeaponLoot(weaponKey, x, z) {\n            const w = weapons[weaponKey];\n            if (!w) return;\n\n            getCachedModel(weaponKey, w.path, (mesh) =\u003e {\n                // Create a main group that holds everything (Not affected by model scaling)\n                const group = new THREE.Group();\n                group.position.set(x, -4.3, z);\n                group.rotation.y = Math.random() * Math.PI * 2;\n\n                // Auto-scale the weapon mesh\n                const bbox = new THREE.Box3().setFromObject(mesh);\n                const size = new THREE.Vector3();\n                bbox.getSize(size);\n\n                // Center the 3D model exactly on the pivot point\n                const center = new THREE.Vector3();\n                bbox.getCenter(center);\n                mesh.position.sub(center);\n\n                const longest = Math.max(size.x, size.y, size.z);\n                const targetLootSize = w.lootTargetSize ?? Math.max(0.75, w.targetSize || 0.75);\n                const fitScale = longest \u003e 0 ? targetLootSize / longest : 0.3;\n\n                // Apply scale to a wrapper, NOT the main group\n                const scaleGroup = new THREE.Group();\n                scaleGroup.scale.setScalar(fitScale);\n                scaleGroup.add(mesh);\n                group.add(scaleGroup);\n\n                mesh.traverse(n =\u003e {\n                    if (n.isMesh) {\n                        n.castShadow = true;\n                        n.frustumCulled = false;\n                        if (n.material) {\n                            n.material.side = THREE.DoubleSide;\n                            n.material.needsUpdate = true;\n                        }\n                    }\n                });\n\n                // Glow light underneath\n                const glow = new THREE.PointLight(0xffcc44, 8, 4);\n                glow.position.set(0, -0.3, 0);\n                group.add(glow);\n\n                // ðŸ”¥ FIXED: Hitbox is attached to the Group, so it NEVER shrinks!\n                const hitBox = new THREE.Mesh(\n                    new THREE.SphereGeometry(1.5, 8, 8),\n                    new THREE.MeshBasicMaterial({ visible: false })\n                );\n                hitBox.name = \u0027loot-weapon\u0027;\n                hitBox.position.set(0, 0.5, 0);\n                group.add(hitBox);\n\n                scene.add(group);\n\n                const entry = {\n                    mesh: group, // The group is what bobs up and down now\n                    hitBox,\n                    type: \u0027weapon\u0027,\n                    data: { weaponKey, name: w.name },\n                    baseY: -4.3,\n                    bobPhase: Math.random() * Math.PI * 2,\n                };\n                hitBox._lootEntry = entry;\n                worldLoot.push(entry);\n                lootHitboxes.push(hitBox);\n                items.push(hitBox);\n            });\n        }\n        function spawnScopeLoot(x, z) {\n            const loader = new GLTFLoader();\n            loader.load(\u0027assets/weapon/scope.glb\u0027, (gltf) =\u003e {\n                const mesh = gltf.scene;\n\n                // Auto-scale\n                const bbox = new THREE.Box3().setFromObject(mesh);\n                const size = new THREE.Vector3();\n                bbox.getSize(size);\n                const longest = Math.max(size.x, size.y, size.z);\n                const fitScale = longest \u003e 0 ? 0.7 / longest : 0.25;\n                mesh.scale.setScalar(fitScale);\n                mesh.position.set(x, -4.3, z);\n                mesh.rotation.y = Math.random() * Math.PI * 2;\n                mesh.frustumCulled = false;\n\n                mesh.traverse(n =\u003e {\n                    if (n.isMesh) {\n                        n.castShadow = false;\n                        n.frustumCulled = false;\n                        if (n.material) {\n                            n.material.side = THREE.DoubleSide;\n                            n.material.needsUpdate = true;\n                        }\n                    }\n                });\n\n                // Cyan glow to distinguish from weapons\n                const glow = new THREE.PointLight(0x00ffcc, 8, 4);\n                glow.position.set(0, 0.5, 0);\n                mesh.add(glow);\n\n                // Hitbox\n                const hitBox = new THREE.Mesh(\n                    new THREE.SphereGeometry(1.2, 8, 8),\n                    new THREE.MeshBasicMaterial({ visible: false })\n                );\n                hitBox.name = \u0027loot-scope\u0027;\n                hitBox.position.set(0, 0.5, 0);\n                mesh.add(hitBox);\n\n                scene.add(mesh);\n\n                const entry = {\n                    mesh, hitBox,\n                    type: \u0027scope\u0027,\n                    data: { label: \u0027Thermal Scope\u0027 },\n                    baseY: -4.3,\n                    bobPhase: Math.random() * Math.PI * 2,\n                };\n                hitBox._lootEntry = entry;\n                worldLoot.push(entry);\n                lootHitboxes.push(hitBox);\n                items.push(hitBox);\n                SCOPE_LOOT_LIST.push(entry);\n            }, undefined, err =\u003e console.warn(\u0027Scope GLB load failed:\u0027, err));\n        }\n\n        // --- Spawn an ammo loot item in the world ---\n        function spawnAmmoLoot(ammoTypeKey, x, z) {\n            const def = ammoLootTypes[ammoTypeKey];\n            if (!def) return;\n\n            // Ammo box: simple geometry (no GLB needed)\n            const group = new THREE.Group();\n            group.position.set(x, -4.4, z);\n\n            // Main box body\n            const box = new THREE.Mesh(\n                new THREE.BoxGeometry(0.35, 0.22, 0.5),\n                new THREE.MeshStandardMaterial({ color: def.color, roughness: 0.5, metalness: 0.4, emissive: def.color, emissiveIntensity: 0.15 })\n            );\n            box.castShadow = true;\n            group.add(box);\n\n            // Stripe on top\n            const stripe = new THREE.Mesh(\n                new THREE.BoxGeometry(0.36, 0.04, 0.52),\n                new THREE.MeshStandardMaterial({ color: 0x111111, roughness: 0.8, metalness: 0.2 })\n            );\n            stripe.position.y = 0.13;\n            group.add(stripe);\n\n            // Glow\n            const glow = new THREE.PointLight(def.glowColor, 6, 3);\n            glow.position.set(0, 0.5, 0);\n            group.add(glow);\n\n            // Hitbox\n            const hitBox = new THREE.Mesh(\n                new THREE.SphereGeometry(1.0, 8, 8),\n                new THREE.MeshBasicMaterial({ visible: false })\n            );\n            hitBox.name = \u0027loot-ammo\u0027;\n            hitBox._lootEntry = null;\n            hitBox.position.set(0, 0.3, 0);\n            group.add(hitBox);\n\n            scene.add(group);\n\n            const entry = {\n                mesh: group,\n                hitBox,\n                type: \u0027ammo\u0027,\n                data: { weaponKey: def.weaponKey, label: def.label, amount: def.amount },\n                baseY: -4.4,\n                bobPhase: Math.random() * Math.PI * 2,\n            };\n            hitBox._lootEntry = entry;\n            worldLoot.push(entry);\n            lootHitboxes.push(entry.hitBox);\n            items.push(hitBox); // register with existing raycaster array\n        }\n\n        function resolveLootEntryFromObject(object) {\n            let node = object;\n            while (node) {\n                if (node._lootEntry) return node._lootEntry;\n                node = node.parent;\n            }\n            return null;\n        }\n\n        function getTargetedLoot(maxDistance = 4.5) {\n            const nearby = [];\n            for (const entry of worldLoot) {\n                if (camera.position.distanceTo(entry.mesh.position) \u003c= maxDistance) {\n                    nearby.push(entry);\n                }\n            }\n            if (nearby.length === 0) return null;\n\n            raycaster.setFromCamera(mouseCenter, camera);\n            const hits = raycaster.intersectObjects(nearby.map(entry =\u003e entry.hitBox), false);\n            for (const hit of hits) {\n                if (hit.distance \u003e maxDistance) continue;\n                const entry = resolveLootEntryFromObject(hit.object);\n                if (entry) return entry;\n            }\n\n            camera.getWorldDirection(_lootRayDirection);\n            let bestEntry = null;\n            let bestScore = 0.92;\n            for (const entry of nearby) {\n                entry.hitBox.getWorldPosition(_lootTargetPos);\n                _lootToTarget.copy(_lootTargetPos).sub(camera.position);\n                const dist = _lootToTarget.length();\n                if (dist \u003c= 0.001) continue;\n                _lootToTarget.divideScalar(dist);\n                const score = _lootToTarget.dot(_lootRayDirection) - dist * 0.015;\n                if (score \u003e bestScore) {\n                    bestScore = score;\n                    bestEntry = entry;\n                }\n            }\n            return bestEntry;\n        }\n\n        // --- Animate all loot (bob + spin) â€” called every frame ---\n        function updateLootAnimations(timeSec) {\n            for (const entry of worldLoot) {\n                if (!entry.mesh) continue;\n                // Skip animation entirely if far from player â€” big performance save\n                const dist = camera.position.distanceTo(entry.mesh.position);\n                if (dist \u003e 25) continue;\n\n                const bobY = Math.sin(timeSec * 1.8 + entry.bobPhase) * 0.10;\n                const baseY = entry.baseY !== undefined ? entry.baseY\n                    : (entry.type === \u0027weapon\u0027 ? -4.6 : -4.4);\n                entry.mesh.position.y = baseY + bobY;\n                entry.mesh.rotation.y += 0.008;\n            }\n        }\n\n        // --- Remove a loot entry from the world and from tracking arrays ---\n        function removeLoot(entry) {\n            scene.remove(entry.mesh);\n            const wi = worldLoot.indexOf(entry);\n            if (wi \u003e -1) worldLoot.splice(wi, 1);\n            const ii = items.indexOf(entry.hitBox);\n            if (ii \u003e -1) items.splice(ii, 1);\n            const li = lootHitboxes.indexOf(entry.hitBox); // â† ADD\n            if (li \u003e -1) lootHitboxes.splice(li, 1);       // â† ADD\n        }\n\n        // --- Try to pick up a loot item the player is looking at ---\n        function tryPickupLoot() {\n            const targetedLoot = getTargetedLoot(4.5);\n            if (targetedLoot) {\n                if (targetedLoot.type === \u0027weapon\u0027) {\n                    const key = targetedLoot.data.weaponKey;\n                    weapons[key].ammo = weapons[key].maxAmmo;\n                    removeLoot(targetedLoot);\n                    unlockWeapon(key);\n                    switchWeapon(key);\n                    showNotification(`Picked up: ${targetedLoot.data.name}`);\n                    playLootPickupSound();\n                }\n                else if (targetedLoot.type === \u0027scope\u0027) {\n                    playerHasScope = true;\n                    removeLoot(targetedLoot);\n                    showNotification(\u0027Thermal Scope equipped - Press T to toggle\u0027);\n                    playLootPickupSound();\n                }\n                else if (targetedLoot.type === \u0027ammo\u0027) {\n                    const key = targetedLoot.data.weaponKey;\n                    if (weapons[key]) {\n                        weapons[key].ammo = Math.min(weapons[key].maxAmmo, weapons[key].ammo + targetedLoot.data.amount);\n                        if (currentWeaponKey === key) {\n                            weaponStats.currentAmmo = Math.min(weaponStats.maxAmmo, weaponStats.currentAmmo + targetedLoot.data.amount);\n                            updateAmmoHUD();\n                        }\n                    }\n                    removeLoot(targetedLoot);\n                    showNotification(`+${targetedLoot.data.amount} ${targetedLoot.data.label}`);\n                    playLootPickupSound();\n                }\n                return true;\n            }\n\n            let closest = null;\n            let closestDist = 4.5; // pickup radius in world units â€” increase if still too hard\n\n            for (const entry of worldLoot) {\n                const dist = camera.position.distanceTo(entry.mesh.position);\n                if (dist \u003c closestDist) {\n                    closestDist = dist;\n                    closest = entry;\n                }\n            }\n\n            if (!closest) return false;\n\n            if (closest.type === \u0027weapon\u0027) {\n                const key = closest.data.weaponKey;\n                weapons[key].ammo = weapons[key].maxAmmo;\n                removeLoot(closest);\n                unlockWeapon(key);\n                switchWeapon(key);\n                showNotification(`ðŸ”« Picked up: ${closest.data.name}`);\n                playLootPickupSound();\n\n            }\n            else if (closest.type === \u0027scope\u0027) {\n                playerHasScope = true;\n                removeLoot(closest);\n                showNotification(\u0027ðŸ”­ Thermal Scope equipped â€” Press T to toggle\u0027);\n                playLootPickupSound();\n            }\n            else if (closest.type === \u0027ammo\u0027) {\n                const key = closest.data.weaponKey;\n                if (weapons[key]) {\n                    weapons[key].ammo = Math.min(weapons[key].maxAmmo, weapons[key].ammo + closest.data.amount);\n                    if (currentWeaponKey === key) {\n                        weaponStats.currentAmmo = Math.min(weaponStats.maxAmmo, weaponStats.currentAmmo + closest.data.amount);\n                        updateAmmoHUD();\n                    }\n                }\n                removeLoot(closest);\n                showNotification(`+${closest.data.amount} ${closest.data.label}`);\n                playLootPickupSound();\n            }\n\n            return true;\n        }\n        // --- Show \"Press E to pick up\" prompt for loot ---\n        function updateLootPrompt() {\n            const entry = getTargetedLoot(4.5);\n            const interactPrompt = document.getElementById(\u0027interact-prompt\u0027);\n\n            if (!entry) {\n                interactPrompt.classList.remove(\u0027visible\u0027);\n                return;\n            }\n\n            interactPrompt.classList.add(\u0027visible\u0027);\n            let content = \u0027\u0027;\n\n            // Common Header Row helper\n            const getHeader = (iconHtml) =\u003e `\n        \u003cdiv class=\"loot-header-row\"\u003e\n            \u003cdiv class=\"loot-prompt-key\"\u003eE\u003c/div\u003e\n            ${iconHtml}\n        \u003c/div\u003e`;\n\n            if (entry.type === \u0027weapon\u0027) {\n                const w = weapons[entry.data.weaponKey];\n                content = `\n            \u003cdiv class=\"loot-prompt-inner\"\u003e\n                ${getHeader(`\u003cimg class=\"loot-prompt-icon\" src=\"${w?.icon || \u0027\u0027}\" onerror=\"this.style.opacity=0\"\u003e`)}\n                \u003cdiv class=\"loot-prompt-name\"\u003e${entry.data.name.toUpperCase()}\u003c/div\u003e\n                \u003cdiv class=\"loot-prompt-stats\"\u003e\n                    \u003cspan style=\"color:white\"\u003eDMG\u003c/span\u003e ${w?.damage ?? \u002724\u0027} \n                    \u003cspan style=\"margin: 0 8px; opacity: 0.3\"\u003e|\u003c/span\u003e \n                    \u003cspan style=\"color:white\"\u003eCAP\u003c/span\u003e ${w?.maxAmmo ?? \u002730\u0027}\n                \u003c/div\u003e\n            \u003c/div\u003e`;\n\n            } else if (entry.type === \u0027ammo\u0027) {\n                content = `\n            \u003cdiv class=\"loot-prompt-inner\"\u003e\n                ${getHeader(`\u003cdiv class=\"loot-prompt-ammo-box\"\u003e\u003c/div\u003e`)}\n                \u003cdiv class=\"loot-prompt-name\"\u003e${entry.data.label.toUpperCase()}\u003c/div\u003e\n                \u003cdiv class=\"loot-prompt-stats\"\u003eREPLENISH +${entry.data.amount} ROUNDS\u003c/div\u003e\n            \u003c/div\u003e`;\n\n            } else if (entry.type === \u0027scope\u0027) {\n                content = `\n            \u003cdiv class=\"loot-prompt-inner\"\u003e\n                ${getHeader(`\u003cdiv style=\"font-size:0.7rem; color:#ff5e00\"\u003eOPTIC ATTACHMENT\u003c/div\u003e`)}\n                \u003cdiv class=\"loot-prompt-name\"\u003eTHERMAL SCOPE\u003c/div\u003e\n                \u003cdiv class=\"loot-prompt-stats\"\u003eTOGGLE [T] WHILE AIMING\u003c/div\u003e\n            \u003c/div\u003e`;\n            }\n\n            interactPrompt.innerHTML = content;\n        }";

