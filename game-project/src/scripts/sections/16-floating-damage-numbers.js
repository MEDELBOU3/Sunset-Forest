export default "        // =========================================================\n        // FLOATING DAMAGE NUMBERS\n        // =========================================================\n        function showDamageNumber(worldPos, amount, isHeadshot) {\n            const projected = worldPos.clone().project(camera);\n            if (projected.z \u003e 1) return; // behind camera, skip\n\n            const sx = (projected.x * 0.5 + 0.5) * window.innerWidth;\n            const sy = (-projected.y * 0.5 + 0.5) * window.innerHeight;\n\n            const el = document.createElement(\u0027div\u0027);\n            el.textContent = (isHeadshot ? \u0027ðŸ’€ \u0027 : \u0027\u0027) + \u0027-\u0027 + Math.ceil(amount);\n            el.style.cssText = `\n        position:fixed;\n        left:${sx}px;\n        top:${sy}px;\n        transform:translate(-50%,-50%);\n        font-family:\u0027Cinzel\u0027,serif;\n        font-size:${isHeadshot ? \u002726px\u0027 : \u002718px\u0027};\n        font-weight:700;\n        color:${isHeadshot ? \u0027#ff2222\u0027 : \u0027#ffcc44\u0027};\n        text-shadow:2px 2px 0 #000;\n        pointer-events:none;\n        z-index:9999;\n        animation: dmgFloat ${isHeadshot ? 1.4 : 1.0}s ease-out forwards;\n    `;\n            document.body.appendChild(el);\n            setTimeout(() =\u003e el.remove(), isHeadshot ? 1400 : 1000);\n        }";

