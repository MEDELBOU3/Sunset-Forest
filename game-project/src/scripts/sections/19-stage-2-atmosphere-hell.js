export default "        // =========================================================\n        // STAGE 2 ATMOSPHERE â€” HELL\n        // =========================================================\n        function applyStage2Atmosphere() {\n            // Sky \u0026 fog\n            scene.background = new THREE.Color(0x08000f);\n            scene.fog = new THREE.Fog(0x1a0008, 6, 50);\n\n            // Sky dome recolor\n            scene.traverse(obj =\u003e {\n                if (obj.isMesh \u0026\u0026 obj.material?.uniforms) {\n                    if (obj.material.uniforms.topColor)\n                        obj.material.uniforms.topColor.value.set(0x04000a);\n                    if (obj.material.uniforms.bottomColor)\n                        obj.material.uniforms.bottomColor.value.set(0x8b0000);\n                    obj.material.uniforms.exponent \u0026\u0026 (obj.material.uniforms.exponent.value = 0.3);\n                }\n            });\n\n            // Lighting overhaul\n            sunLight.color.set(0xff1800);\n            sunLight.intensity = 2.8;\n            rimLight.color.set(0x660000);\n            rimLight.intensity = 1.2;\n            scene.children.forEach(c =\u003e {\n                if (c.isAmbientLight) { c.color.set(0x1a0005); c.intensity = 0.9; }\n                if (c.isHemisphereLight) {\n                    c.color.set(0x8b0000);\n                    c.groundColor.set(0x050000);\n                    c.intensity = 0.7;\n                }\n            });\n\n            // Ground charred black\n            ground.material.color.set(0x080404);\n            ground.material.roughness = 1.0;\n\n            // Lava pools\n            spawnLavaPools();\n\n            // Hell fire column particles\n            spawnHellFireColumns();\n\n            // Floating ash particles (replace dust)\n            spawnAshParticles();\n\n            // Flickering hell lights scattered around\n            spawnHellLights();\n\n            // Stage 2 zombies are faster â€” patch the constant live\n            zombies.forEach(z =\u003e { z.speed *= 1.5; });\n        }\n\n        function spawnLavaPools() {\n            const pools = [\n                [22, 18], [-18, 28], [35, -15],\n                [-28, -12], [8, -38], [-42, 8]\n            ];\n            pools.forEach(([x, z]) =\u003e {\n                const pool = new THREE.Mesh(\n                    new THREE.CircleGeometry(2.5 + Math.random() * 1.5, 12),\n                    new THREE.MeshBasicMaterial({\n                        color: 0xff3300, transparent: true, opacity: 0.75,\n                        blending: THREE.AdditiveBlending, depthWrite: false\n                    })\n                );\n                pool.rotation.x = -Math.PI / 2;\n                pool.position.set(x, -4.85, z);\n                pool.matrixAutoUpdate = false;\n                pool.updateMatrix();\n                scene.add(pool);\n\n                const core = new THREE.Mesh(\n                    new THREE.CircleGeometry(1.0, 8),\n                    new THREE.MeshBasicMaterial({\n                        color: 0xffaa00, transparent: true, opacity: 0.9,\n                        blending: THREE.AdditiveBlending, depthWrite: false\n                    })\n                );\n                core.rotation.x = -Math.PI / 2;\n                core.position.set(x, -4.83, z);\n                core.matrixAutoUpdate = false;\n                core.updateMatrix();\n                scene.add(core);\n            });\n        }\n\n        function spawnHellFireColumns() {\n            const positions = [\n                [30, 10], [-20, 35], [45, -25]\n            ];\n            positions.forEach(([x, z]) =\u003e {\n                const count = 40;\n                const geo = new THREE.BufferGeometry();\n                const pos = new Float32Array(count * 3);\n                const vels = [];\n                for (let i = 0; i \u003c count; i++) {\n                    const r = Math.random() * 1.2;\n                    const a = Math.random() * Math.PI * 2;\n                    pos[i * 3] = x + Math.cos(a) * r;\n                    pos[i * 3 + 1] = -5 + Math.random() * 8;\n                    pos[i * 3 + 2] = z + Math.sin(a) * r;\n                    vels.push({\n                        speedY: 0.8 + Math.random() * 1.2,\n                        swaySpeed: 2 + Math.random() * 3,\n                        swayAmount: 0.15 + Math.random() * 0.3,\n                        phase: Math.random() * Math.PI * 2,\n                        baseX: x, baseZ: z\n                    });\n                }\n                geo.setAttribute(\u0027position\u0027, new THREE.BufferAttribute(pos, 3));\n                const mat = new THREE.PointsMaterial({\n                    size: 0.5, color: 0xff4400,\n                    map: createGlowTexture(false),\n                    transparent: true, opacity: 0.85,\n                    blending: THREE.AdditiveBlending, depthWrite: false\n                });\n                const pts = new THREE.Points(geo, mat);\n                scene.add(pts);\n                particleSystems.push({\n                    points: pts, velocities: vels, count,\n                    isFireColumn: true, topY: 3, baseX: x, baseZ: z\n                });\n            });\n        }\n        function spawnAshParticles() {\n            const count = 700;\n            const geo = new THREE.BufferGeometry();\n            const pos = new Float32Array(count * 3);\n            const vels = [];\n            for (let i = 0; i \u003c count; i++) {\n                pos[i * 3] = (Math.random() - 0.5) * 200;\n                pos[i * 3 + 1] = -5 + Math.random() * 25;\n                pos[i * 3 + 2] = (Math.random() - 0.5) * 200;\n                vels.push({\n                    speedY: 0.02 + Math.random() * 0.06,\n                    swaySpeed: 0.3 + Math.random() * 0.8,\n                    swayAmount: 0.08 + Math.random() * 0.25,\n                    phase: Math.random() * Math.PI * 2\n                });\n            }\n            geo.setAttribute(\u0027position\u0027, new THREE.BufferAttribute(pos, 3));\n            const mat = new THREE.PointsMaterial({\n                size: 0.18, color: 0x888888,\n                transparent: true, opacity: 0.5,\n                blending: THREE.AdditiveBlending, depthWrite: false\n            });\n            const pts = new THREE.Points(geo, mat);\n            scene.add(pts);\n            particleSystems.push({ points: pts, velocities: vels, count });\n        }\n\n        function spawnHellLights() {\n            const hellLightPositions = [\n                [-15, 20], [25, -10], [-30, -25], [40, 30], [-5, -50]\n            ];\n            hellLightPositions.forEach(([x, z]) =\u003e {\n                const l = new THREE.PointLight(0xff0000, 6, 25);\n                l.position.set(x, 2, z);\n                scene.add(l);\n            });\n        }\n\n        // Fallback for stage 3+\n        function applyStageNAtmosphere(stage) {\n            scene.fog.near = Math.max(4, 8 - stage);\n            scene.fog.far = Math.max(30, 50 - stage * 3);\n            sunLight.intensity = Math.min(4.0, 2.8 + stage * 0.2);\n            showNotification(`âš ï¸ Stage ${stage} â€” Darkness deepens...`);\n        }\n\n        // CUSTOM CURSOR LOGIC\n        const customCursor = document.getElementById(\u0027cursor\u0027);\n        document.addEventListener(\u0027mousemove\u0027, e =\u003e {\n            // Only show custom cursor when locked in game, hide in all menus for speed\n            if (controls.isLocked) {\n                customCursor.style.left = e.clientX + \u0027px\u0027;\n                customCursor.style.top = e.clientY + \u0027px\u0027;\n                customCursor.style.display = \u0027block\u0027;\n            } else {\n                customCursor.style.display = \u0027none\u0027;\n            }\n        });\n\n        // Consume on click\n        document.getElementById(\u0027slot-mushroom\u0027).addEventListener(\u0027click\u0027, () =\u003e {\n            if (mushroomCount \u003c= 0) return;\n            mushroomCount--;\n            updateInventoryPanel();\n\n            // THIS is what grows your health\n            playerStats.health = Math.min(playerStats.maxHealth, playerStats.health + 30);\n            updateHealthHUD();\n\n            if (slurpSound.buffer) {\n                if (slurpSound.isPlaying) slurpSound.stop();\n                slurpSound.play();\n            }\n\n            showNotification(\u0027+30 HP â€” Mushroom consumed\u0027);\n        });\n\n        // Init\n        updateInventoryPanel();";

