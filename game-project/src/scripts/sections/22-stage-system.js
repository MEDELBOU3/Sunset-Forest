export default "        // =========================================================\n        // STAGE SYSTEM\n        // =========================================================\n        let currentStage = 1;\n        let gateSpawned = false;\n        let gateMesh = null;\n        let stageTransitioning = false;\n        let glowPathActive = false;\n        const pathOrbs = [];\n        let pathOrbsGroup = null;\n        let waveZombiesKilled = 0;   // â† ADD THIS\n        const GATE_POSITION = new THREE.Vector3(65, -5, 0);\n\n        const zombieLoader = new GLTFLoader();\n        zombieLoader.load(ZOMBIE_PATH, (gltf) =\u003e {\n            zombieTemplate = gltf;\n            setTimeout(() =\u003e startNextWave(), 4000);\n        }, undefined, err =\u003e console.error(\u0027Zombie load failed:\u0027, err));\n\n        function startNextWave() {\n            currentWave++;\n            waveSpawnTotal = 5 + (currentWave - 1) * 3;\n            waveSpawnInterval = Math.max(0.6, 1.8 - currentWave * 0.12);\n            waveZombiesSpawned = 0;\n            waveZombiesLeft = waveSpawnTotal;\n            waveZombiesKilled = 0;   // â† ADD THIS\n            waveSpawnTimer = 0;\n            waveActive = true;\n            betweenWaveTimer = 0;\n\n            waveAnnTitle.textContent = \u0027WAVE \u0027 + currentWave;\n            const phrases = [\u0027The dead are rising...\u0027, \u0027They\\\u0027re coming for you...\u0027, \u0027More emerge from the dark...\u0027, \u0027No rest for the living...\u0027, \u0027The horde grows stronger...\u0027, \u0027Run. Or fight.\u0027];\n            waveAnnSub.textContent = phrases[Math.min(currentWave - 1, phrases.length - 1)];\n            waveAnnounce.classList.remove(\u0027show\u0027); void waveAnnounce.offsetWidth; waveAnnounce.classList.add(\u0027show\u0027);\n            waveNumEl.textContent = currentWave;\n            updateZombieCountHUD();\n        }\n\n        function updateZombieCountHUD() { zombieCountEl.textContent = zombies.length; }\n\n        function spawnZombie() {\n            if (zombies.length \u003e= MAX_ZOMBIES) return;\n            if (!zombieTemplate) return;\n            const arcDeg = SPAWN_ARCS[Math.floor(Math.random() * SPAWN_ARCS.length)] + (Math.random() - 0.5) * 30;\n            const arcRad = arcDeg * Math.PI / 180;\n            const dist = ZOMBIE_SPAWN_RADIUS + Math.random() * 20;\n            const playerPos = camera.position;\n            let sx = Math.max(-90, Math.min(90, playerPos.x + Math.sin(arcRad) * dist));\n            let sz = Math.max(-90, Math.min(90, playerPos.z + Math.cos(arcRad) * dist));\n\n            const clone = SkeletonUtils_clone(zombieTemplate.scene);\n\n            // Use fixed scale â€” bbox approach is unreliable for SkinnedMesh before first render\n            const fitScale = ZOMBIE_SCALE;\n            const visibleScale = 1.3; // Start with 0.5; if too big/small, adjust this number\n            clone.scale.set(visibleScale, visibleScale, visibleScale);\n\n            clone.position.set(sx, GROUND_Y, sz);\n            clone.castShadow = true;\n            clone.frustumCulled = false; // CRITICAL: Prevents zombie from disappearing when turned away\n            markZombieThermalLayer(clone);\n            clone.traverse(n =\u003e {\n                if (n.isMesh) {\n                    n.castShadow = true;\n                    n.receiveShadow = true;\n                    n.frustumCulled = false;\n                }\n            });\n            scene.add(clone);\n\n            if (zombies.length \u003e 0) console.log(\"Zombies in scene: \" + zombies.length);\n\n            const mixer = new THREE.AnimationMixer(clone);\n            const clip = zombieTemplate.animations[0];\n            let action = null;\n            if (clip) { action = mixer.clipAction(clip); action.play(); }\n\n            // Health bar DOM\n            const hbWrap = document.createElement(\u0027div\u0027);\n            hbWrap.className = \u0027zombie-healthbar-wrap\u0027;\n            hbWrap.innerHTML = `\u003cdiv class=\"zombie-hb-label\"\u003eZOMBIE\u003c/div\u003e\u003cdiv class=\"zombie-hb-bg\"\u003e\u003cdiv class=\"zombie-hb-fill\" style=\"width:100%\"\u003e\u003c/div\u003e\u003c/div\u003e`;\n            zombieHbarsContainer.appendChild(hbWrap);\n            const hbFill = hbWrap.querySelector(\u0027.zombie-hb-fill\u0027);\n\n            // ===================== FIX: Positional audio per zombie â€” fades with distance =====================\n            let zombieSound = null;\n            if (zombieGroanBuffer) {\n                zombieSound = new THREE.PositionalAudio(listener);\n                zombieSound.setBuffer(zombieGroanBuffer);\n                zombieSound.setLoop(true);\n                zombieSound.setRefDistance(8);       // Full volume within 8 units\n                zombieSound.setRolloffFactor(2.5);   // Quick fade with distance\n                zombieSound.setMaxDistance(80);      // Inaudible beyond 80 units\n                zombieSound.setVolume(0.9);\n                zombieSound.play();\n                clone.add(zombieSound);\n            }\n\n            const zombie = {\n                mesh: clone,\n                mixer, action,\n                visualHeight: ZOMBIE_VISUAL_HEIGHT,\n                hp: ZOMBIE_MAX_HP, maxHp: ZOMBIE_MAX_HP,\n                isDead: false, deathTimer: 0,\n                isAlerted: false, // NEW: Start as false\n                spawnPoint: new THREE.Vector3(sx, GROUND_Y, sz), // Store where they spawned\n                lastAttackTime: 0,\n                hbWrap, hbFill,\n                sound: zombieSound,\n                fitScale,                             // â† stored for death animation\n                speed: ZOMBIE_SPEED_BASE + Math.random() * 0.6 + (currentWave - 1) * 0.15,\n                startDelay: Math.random() * 0.8,\n            };\n            zombies.push(zombie);\n            clone.traverse(n =\u003e { zombieMeshMap.set(n, zombie); });\n            updateZombieCountHUD();\n        }\n\n        function SkeletonUtils_clone(source) {\n            const clone = source.clone(true);\n            const sourceBones = [], cloneBones = [];\n            source.traverse(n =\u003e { if (n.isBone) sourceBones.push(n); });\n            clone.traverse(n =\u003e { if (n.isBone) cloneBones.push(n); });\n            clone.traverse(n =\u003e {\n                if (n.isSkinnedMesh) {\n                    const newBones = n.skeleton.bones.map(b =\u003e {\n                        const idx = sourceBones.indexOf(b);\n                        return idx !== -1 ? cloneBones[idx] : b;\n                    });\n                    n.skeleton = new THREE.Skeleton(newBones, n.skeleton.boneInverses);\n                    n.bind(n.skeleton, n.matrixWorld);\n                }\n            });\n            return clone;\n        }\n\n        // Call this after spawnZombie() to visualize the head hitbox â€” REMOVE WHEN DONE\n        function debugShowHeadzone(zombie) {\n            const height = zombie.visualHeight ?? ZOMBIE_VISUAL_HEIGHT;\n            const headH = height * (0.98 - 0.84);\n            const headMidY = zombie.mesh.position.y + height * 0.91; // midpoint\n\n            const box = new THREE.Mesh(\n                new THREE.BoxGeometry(0.4, headH, 0.4),\n                new THREE.MeshBasicMaterial({\n                    color: 0xff0000,\n                    wireframe: true,\n                    transparent: true,\n                    opacity: 0.8\n                })\n            );\n            box.position.set(\n                zombie.mesh.position.x,\n                headMidY,\n                zombie.mesh.position.z\n            );\n            scene.add(box);\n            zombie._debugBox = box; // store ref so you can remove it on death\n        }\n\n        function spawnDeathEffect(pos) {\n            const count = 40;\n            const geo = new THREE.BufferGeometry();\n            const positions = new Float32Array(count * 3);\n            const velocitiesArr = [];\n            for (let i = 0; i \u003c count; i++) {\n                positions[i * 3] = pos.x; positions[i * 3 + 1] = pos.y + 1; positions[i * 3 + 2] = pos.z;\n                velocitiesArr.push({ vx: (Math.random() - 0.5) * 6, vy: Math.random() * 5 + 1, vz: (Math.random() - 0.5) * 6, life: 1.0, decay: 0.6 + Math.random() * 0.8 });\n            }\n            geo.setAttribute(\u0027position\u0027, new THREE.BufferAttribute(positions, 3));\n            const mat = new THREE.PointsMaterial({ size: 0.9, color: 0x44ff66, transparent: true, opacity: 0.85, blending: THREE.AdditiveBlending, depthWrite: false });\n            const pts = new THREE.Points(geo, mat);\n            scene.add(pts);\n            let elapsed = 0;\n            function tickEffect(dt) {\n                elapsed += dt;\n                const arr = pts.geometry.attributes.position.array;\n                let alive = false;\n                for (let i = 0; i \u003c count; i++) {\n                    const v = velocitiesArr[i]; v.life -= v.decay * dt;\n                    if (v.life \u003c= 0) continue; alive = true;\n                    arr[i * 3] += v.vx * dt; arr[i * 3 + 1] += v.vy * dt; arr[i * 3 + 2] += v.vz * dt; v.vy -= 6 * dt;\n                }\n                pts.geometry.attributes.position.needsUpdate = true;\n                mat.opacity = Math.max(0, elapsed \u003c 0.3 ? 0.85 : 0.85 * (1 - (elapsed - 0.3) / 1.2));\n                if (!alive || elapsed \u003e 1.5) {\n                    scene.remove(pts); geo.dispose(); mat.dispose();\n                    deathEffects.splice(deathEffects.indexOf(tickFn), 1);\n                }\n            }\n            const tickFn = tickEffect;\n            deathEffects.push(tickFn);\n        }\n\n        function triggerBloodFlash() {\n            bloodFlash.classList.remove(\u0027hit\u0027); void bloodFlash.offsetWidth; bloodFlash.classList.add(\u0027hit\u0027);\n        }\n\n        function killZombie(zombie) {\n            if (zombie.isDead) return;\n            if (thermalActive) {\n                zombie.mesh.traverse(n =\u003e {\n                    if (thermalOriginalMaterials.has(n)) {\n                        n.material = thermalOriginalMaterials.get(n);\n                        thermalOriginalMaterials.delete(n);\n                    }\n                });\n            }\n            zombie.isDead = true;\n            zombie.deathTimer = 0;\n            killCount++;\n            waveZombiesKilled++;   // â† ADD THIS\n            scoreValEl.textContent = killCount;\n            spawnDeathEffect(zombie.mesh.position);\n            // Tier 4 â€” Audio Pooling for screams\n            killScreamPool.play(zombie.mesh, 0.85 + Math.random() * 0.3);\n            \n            if (zombie.action) zombie.action.stop();\n            if (zombie.sound) {\n                if (zombie.sound.isPlaying) zombie.sound.stop();\n                zombie.mesh.remove(zombie.sound);\n            }\n            zombie.hbWrap.remove();\n\n            // â”€â”€ CHECK WAVE CLEAR RIGHT HERE â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€\n            checkWaveClear();   // â† ADD THIS\n        }\n\n        function checkWaveClear() {\n            // All spawned zombies for this wave are dead\n            if (!waveActive) return;\n            if (waveZombiesSpawned \u003c waveSpawnTotal) return;\n            if (waveZombiesKilled \u003c waveSpawnTotal) return;\n\n            waveActive = false;\n            console.log(\u0027Wave clear! Stage:\u0027, currentStage, \u0027Spawned:\u0027, waveZombiesSpawned, \u0027Killed:\u0027, waveZombiesKilled);\n\n            if (!gateSpawned \u0026\u0026 !stageTransitioning) {\n                showNotification(\u0027âœ… All zombies eliminated!\u0027);\n                setTimeout(() =\u003e spawnStagegate(), 2000);\n            }\n        }\n\n        function damagePlayer(amount) {\n            if (playerDead) return;\n            playerStats.health = Math.max(0, playerStats.health - amount);\n            updateHealthHUD(); triggerBloodFlash(); triggerDamageVignette();\n            if (playerStats.health \u003c= 0) {\n                playerDead = true;\n\n                // ===================== FIX: Play player death scream =====================\n                if (deathScreamSound.buffer \u0026\u0026 !deathScreamSound.isPlaying) deathScreamSound.play();\n\n                controls.unlock();\n                setTimeout(() =\u003e { deathScreen.classList.add(\u0027visible\u0027); }, 800);\n            }\n        }\n\n        document.getElementById(\u0027respawn-btn\u0027).addEventListener(\u0027click\u0027, () =\u003e {\n            exitArmoredCar(true);\n            playerDead = false;\n            playerStats.health = playerStats.maxHealth;\n            playerStats.stamina = playerStats.maxStamina;\n            updateHealthHUD(); updateStaminaHUD();\n            weaponStats.currentAmmo = weaponStats.maxAmmo;\n            weaponStats.isReloading = false;\n            updateAmmoHUD();\n            killCount = 0; scoreValEl.textContent = 0;\n            currentWave = 0; waveActive = false; betweenWaveTimer = 0;\n\n            for (const z of zombies) {\n                if (z.sound \u0026\u0026 z.sound.isPlaying) z.sound.stop();\n                scene.remove(z.mesh);\n                z.mesh.traverse(n =\u003e zombieMeshMap.delete(n));\n                z.hbWrap.remove();\n            }\n            zombies.length = 0;\n            zombieMeshMap.clear();\n            deathEffects.length = 0;\n            updateZombieCountHUD();\n            camera.position.set(0, -2.03, 0);\n            deathScreen.classList.remove(\u0027visible\u0027);\n            setTimeout(() =\u003e {\n                lockControls();\n                setTimeout(() =\u003e startNextWave(), 3000);\n            }, 300);\n\n            waveZombiesKilled = 0;   // â† ADD alongside the other resets\n            currentStage = 1;\n            gateSpawned = false;\n\n            currentStage = 1;\n            gateSpawned = false;\n            stageTransitioning = false;\n            if (gateMesh) { scene.remove(gateMesh); gateMesh = null; }\n            removeGlowPath();\n\n            // Reset stage 1 atmosphere\n            scene.background = new THREE.Color(0xc8a0c0);\n            scene.fog = new THREE.Fog(0xd0b0c8, 18, 95);\n            sunLight.color.set(0xffd6a5); sunLight.intensity = 2.2;\n            rimLight.color.set(0xc8a8d4); rimLight.intensity = 0.5;\n            ground.material.color.set(0x1a3316);\n        });\n\n        const _zDir = new THREE.Vector3();\n        const _playerPos2D = new THREE.Vector3();\n        const deathEffects = [];\n\n        let zombieLogicTimer = 0;\n        const ZOMBIE_LOGIC_INTERVAL = 0.2; // 200ms throttle (Tier 5)\n\n        function updateZombies(delta, timeSec) {\n            if (!controls.isLocked || playerDead) return;\n\n            // 1. Spawn logic with Hard Cap\n            const MAX_ZOMBIES = 20;\n            if (waveActive \u0026\u0026 waveZombiesSpawned \u003c waveSpawnTotal \u0026\u0026 zombies.length \u003c MAX_ZOMBIES) {\n                waveSpawnTimer -= delta;\n                if (waveSpawnTimer \u003c= 0) {\n                    spawnZombie();\n                    if (thermalActive) {\n                        const z = zombies[zombies.length - 1];\n                        z.mesh.traverse(n =\u003e {\n                            if (n.isMesh \u0026\u0026 n.material \u0026\u0026 !thermalOriginalMaterials.has(n)) {\n                                thermalOriginalMaterials.set(n, n.material);\n                                n.material = thermalZombieMat;\n                            }\n                        });\n                    }\n                    waveZombiesSpawned++;\n                    waveSpawnTimer = waveSpawnInterval;\n                }\n            }\n\n            // Tick death effects\n            for (const fn of [...deathEffects]) fn(delta);\n\n            const now = performance.now() / 1000;\n            _playerPos2D.copy(camera.position).setY(GROUND_Y);\n\n            // Periodically sort zombies and run expensive logic (Tier 5)\n            zombieLogicTimer += delta;\n            const runHeavyLogic = zombieLogicTimer \u003e= ZOMBIE_LOGIC_INTERVAL;\n            if (runHeavyLogic) {\n                zombieLogicTimer = 0;\n                // 2. Sort zombies by distance so we only process UI for the closest ones\n                zombies.sort((a, b) =\u003e a.mesh.position.distanceToSquared(camera.position) - b.mesh.position.distanceToSquared(camera.position));\n            }\n\n            for (let i = zombies.length - 1; i \u003e= 0; i--) {\n                const z = zombies[i];\n                const d2 = z.mesh.position.distanceToSquared(camera.position);\n\n                // Optimization (Tier 3): Sleep distant zombies, skip visibility/HUD\n                if (d2 \u003e 2500) { // 50m\n                    if (z.mesh.visible) { z.mesh.visible = false; z.hbWrap.style.display = \u0027none\u0027; }\n                    continue;\n                } else if (!z.mesh.visible) {\n                    z.mesh.visible = true;\n                }\n\n                // AI Aggro \u0026 Movement (Tick every frame)\n                if (z.isDead) {\n                    z.deathTimer += delta;\n                    z.mesh.position.y -= delta * 1.2;\n                    z.mesh.scale.y = Math.max(0, z.fitScale * (1 - z.deathTimer / 1.2));\n                    if (z.deathTimer \u003e 1.4) {\n                        scene.remove(z.mesh);\n                        z.mesh.traverse(n =\u003e zombieMeshMap.delete(n));\n                        zombies.splice(i, 1);\n                        updateZombieCountHUD();\n                    }\n                    z.hbWrap.style.display = \u0027none\u0027;\n                    continue;\n                }\n                \n                // Only run animation mixers for close zombies (Tier 3)\n                if (d2 \u003c 900) { // 30m\n                    z.mixer.update(delta);\n                }\n\n                const distToPlayer = Math.sqrt(d2);\n                if (!z.isAlerted \u0026\u0026 distToPlayer \u003c 25) {\n                    z.isAlerted = true;\n                    if (z.sound \u0026\u0026 !z.sound.isPlaying) { z.sound.setVolume(1.5); z.sound.play(); }\n                }\n\n                if (z.isAlerted) {\n                    _zDir.copy(_playerPos2D).sub(z.mesh.position);\n                    _zDir.y = 0;\n                    const dist = _zDir.length();\n                    z.mesh.rotation.y = Math.atan2(_zDir.x, _zDir.z);\n\n                    if (dist \u003e ZOMBIE_ATTACK_RANGE) {\n                        _zDir.normalize();\n                        z.mesh.position.x += _zDir.x * z.speed * delta;\n                        z.mesh.position.z += _zDir.z * z.speed * delta;\n                        const zProbe = { x: z.mesh.position.x, z: z.mesh.position.z };\n                        CollisionSystem.resolvePoint(zProbe, CollisionSystem.ZOMBIE_R);\n                        z.mesh.position.x = zProbe.x; z.mesh.position.z = zProbe.z;\n                    } else if (now - z.lastAttackTime \u003e= ZOMBIE_ATTACK_RATE) {\n                        z.lastAttackTime = now;\n                        damagePlayer(ZOMBIE_ATTACK_DAMAGE);\n                    }\n                } else {\n                    z.mesh.rotation.y += Math.sin(timeSec) * 0.01;\n                }\n\n                // 3. UI Optimization (Tier 3): Limit health bars to top 3 nearest\n                if (i \u003c 3) {\n                    updateZombieHUD(z, distToPlayer);\n                } else {\n                    z.hbWrap.style.display = \u0027none\u0027;\n                }\n            }\n            CollisionSystem.separateZombies(zombies, camera.position);\n        }\n        // Helper to keep updateZombies clean\n        function updateZombieHUD(z, distToPlayer) {\n            const worldPos = z.mesh.position.clone();\n            worldPos.y = GROUND_Y + ZOMBIE_VISUAL_HEIGHT + 0.5;\n            const projected = worldPos.project(camera);\n\n            if (projected.z \u003c 1 \u0026\u0026 Math.abs(projected.x) \u003c 1.1 \u0026\u0026 Math.abs(projected.y) \u003c 1.1) {\n                const sx = (projected.x * 0.5 + 0.5) * window.innerWidth;\n                const sy = (-projected.y * 0.5 + 0.5) * window.innerHeight;\n                z.hbWrap.style.display = \u0027block\u0027;\n                z.hbWrap.style.left = sx + \u0027px\u0027; z.hbWrap.style.top = sy + \u0027px\u0027;\n                const scaleFactor = Math.max(0.4, Math.min(1.2, 12 / distToPlayer));\n                z.hbWrap.style.transform = `translate(-50%, -100%) scale(${scaleFactor})`;\n                z.hbFill.style.width = (z.hp / z.maxHp * 100) + \u0027%\u0027;\n            } else {\n                z.hbWrap.style.display = \u0027none\u0027;\n            }\n        }\n        function damageZombie(zombie, amount) {\n            if (zombie.isDead) return;\n            zombie.hp -= amount;\n            // Tier 4: Use bullet hit pool for impact sounds\n            bulletHitPool.play(zombie.mesh);\n            if (zombie.hp \u003c= 0) killZombie(zombie);\n        }\n\n        function createDarkZone(x, z, radius) {\n            const geo = new THREE.SphereGeometry(radius, 32, 32);\n            const mat = new THREE.MeshBasicMaterial({\n                color: 0x050505, // Deep dark grey\n                transparent: true,\n                opacity: 0.6,\n                side: THREE.BackSide,\n                fog: false // Disable normal fog inside the zone\n            });\n            const zone = new THREE.Mesh(geo, mat);\n            zone.position.set(x, 0, z);\n            scene.add(zone);\n        }";

